<!--[if lt IE 9]>
script(src='//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.3/html5shiv.min.js')
script(src='//cdnjs.cloudflare.com/ajax/libs/es5-shim/4.5.9/es5-shim.min.js')
script(src='//cdnjs.cloudflare.com/ajax/libs/respond.js/1.4.2/respond.min.js')
script(src='//cdnjs.cloudflare.com/ajax/libs/rem/1.3.4/js/rem.min.js')
script(src='//cdnjs.cloudflare.com/ajax/libs/flexibility/2.0.1/flexibility.js')
<![endif]-->

script.
  ;(function() {
    'use strict';
    /*
        Bug workaround for IE10 in Windows Phone 8 versions older than Update 3 (a.k.a. GDR3)
        that don’t differentiate device width from viewport width in @-ms-viewport at-rules.
    */
    if (navigator.userAgent.match(/IEMobile\/10\.0/)) {
      var msvp = document.createElement('style');
      msvp.appendChild(document.createTextNode('@-ms-viewport { width:auto !important }'));
      document.head.appendChild(msvp);
    }
  })();
